# データ活用TにおけるAI駆動開発の取り組み

作成者: ビジネスソリューションG データ活用T 菊池翼

---

# 今日のゴール

この発表では2つのことをお伝えします。

1. AIを使った業務効率化のノウハウを共有する
2. データ活用チームの有志で行っているAI駆動開発の取り組みを紹介する

---

# AI駆動開発とは

AIを開発に活用するアプローチはいくつかある：

- **AIチャット（Copilot Chatなど）** — 質問すると回答してくれる。「聞く → 答えをもらう → 自分で適用する」
- **コード補完（Copilot補完機能など）** — コードを書いている最中に続きを提案してくれる
- **コーディングエージェント（Claude Codeなど）** — タスク全体を自律的に実行する。「指示を出す → AIが調べて・書いて・テストする」

---

# Claude Codeとは

Anthropic社が提供するコーディングエージェント。ターミナル、VS Code、デスクトップアプリ、ブラウザ（claude.ai/code）など多くの環境で使える。

特徴：

- 自然言語で指示するだけで、コードの生成・編集・実行をまとめてやってくれる
- ファイルの読み書き、コマンド実行、Web検索などのツールを標準搭載している
- プロジェクトのコード全体を理解した上で作業する

---

# Claude Codeの導入効果 — 数字で見る

Anthropic社内のエンジニアを対象にした調査結果（2025年2月〜8月）：

- エンジニアの平均生産性が **+50%** 向上
- 日常的にClaude Codeを使うエンジニアが28%→ **59%** に増加
- AIがなければ着手されなかったタスクが全体の **27%**
- 手動では45分以上かかるタスクを1回のやり取りで完了するケースも

企業での導入事例：
- Palo Alto Networks: 2,000人の開発者に導入し、平均 **25%**（最大40%）の生産性向上

出典: Anthropic "How AI Is Transforming Work at Anthropic" (2025), AWS/Palo Alto Networks Case Study

---

# Claude Codeの特徴的な機能

## CLAUDE.md（プロジェクトルール）

プロジェクトのルールや規約をmarkdownで書いておくと、AIが毎回それに従って作業する。口頭の指示ではなく、ドキュメントとしてルールを管理する。

## MCP Server（外部ツール連携）

AIが外部のツールやサービスにアクセスできるようになる仕組み。ドキュメント参照、Excel操作、外部APIの呼び出しなどをAIが直接実行できる。

## Skill（カスタム自動化）

よく使うワークフローをテンプレート化して、AIに繰り返し実行させる仕組み。

## hooks（自動処理）

AIの操作の特定のタイミングで自動的にスクリプトを実行する仕組み。フォーマッターの自動実行、危険なコマンドのブロック、ポリシーの強制などに使える。

---

# Datadresserの全体アーキテクチャ

構成要素：

- **Excel パラメータシート** — 各リソース（Database, Warehouse, Role等）の設定値を定義
- **Python** — ExcelからCSVへの変換処理。リソースごとに変換処理を持つ
- **Terraform** — CSVの値をもとにSnowflakeリソースをプロビジョニング
- **GitHub Actions** — CI/CD（pytest、Terraformプラン、dbtテスト等）

---

# 活用①: コード生成

ExcelパラメータシートからTerraform/Pythonコードを生成する作業にClaude Codeを活用。

実際の流れ：
1. 「Databaseリソース用の変換処理を作って」と指示
2. Claude Codeが既存の変換処理のパターンと設計書を読み取り、新しい変換処理を生成
3. テストコードも合わせて生成
4. 開発者がレビューして修正を指示

プロジェクトのコード全体を理解した上で生成するので、既存のコードスタイルや規約に沿ったコードが出てくる。

---

# 活用②: PRレビュー

PR作成時にClaude Codeが自動でレビューするSkillを導入。

レビューの観点：
- コーディング規約への準拠
- バグの可能性
- 対応する設計書の更新

人間のレビューの前にAIレビューが入ることで、ケアレスミスが事前に潰され、人間のレビューでは設計やビジネスロジックに集中できる。

---

# 活用③: パラメータシート自動生成 — 仕組み

Snowflakeの公式ドキュメントを参照し、リソースのパラメータシート（Excel）を自動生成するSkill。

仕組み：
1. 「Databaseのパラメータシートを作って」とSkillを呼び出す
2. MCP Server（snowflake_docs_mcp）がSnowflakeの公式ドキュメントを検索・取得
3. ドキュメントから必要なパラメータを抽出
4. MCP Server（excel_mcp）でExcelファイルを生成

Web検索 → ドキュメント読解 → Excel操作 を人間が手作業でやると数十分かかる作業が、AIが一気通貫で自動実行する。

---

# ライブデモ: Databaseパラメータシートの自動生成

これから実際にClaude Codeを使って、SnowflakeのDatabaseリソースのパラメータシートを自動生成するデモを行います。

注目ポイント：
- 自然言語の指示だけで作業が始まること
- Snowflakeの公式ドキュメントを自動で参照すること
- 最終的にExcelファイルが自動生成されること

---

# デモの振り返り

（デモ後にポイントを振り返る）

ポイント：
- 人間がやったのは「指示を出す」ことだけ
- Snowflakeのドキュメントの検索・読解・情報抽出はAIが実行した
- Excelのシート作成・書込はMCP経由でAIが実行した
- この一連の流れがSkillとして再利用可能になっている

---

# AIに正しく動いてもらうための工夫

## Claude Codeができる操作を把握する

Claude Codeはテキストファイルの読み書き、Web検索、bashやプログラムの実行などが標準で可能。一方でExcel操作など標準では対応していない作業もあるので、MCP Serverで拡張するなど工夫が必要。

## ルールを明文化する（CLAUDE.md）

「こう書いて」と毎回指示するのではなく、プロジェクトのルールをCLAUDE.mdに書いておく。AIが毎回参照するので、一貫性のあるアウトプットが出る。

## 繰り返す作業はSkillにする

一度うまくいったワークフローはSkillとしてテンプレート化する。品質が安定し、チーム内で共有もしやすくなる。

---

# 今後の取り組み

## Datadresserの進化
- 実案件での本番運用に耐えうるクオリティを目指す
- 最終的には、要件定義書をインプットとするだけで基盤が構築される状態が理想

## 効果の定量化
- AI活用による工数削減効果を定量的に測定する
- 今後のAI活用を推進するための判断材料にしたい

## AI活用の裾野を広げる
- 自チームだけでなく、より多くの人がAIを使うことで業務に活かせるアイデアを増やしていきたい
- 4月下旬からBS本部でAIワークショップを主催